before_script:
  - npm install
  - npm start
# Does not work because of import statement. ES6?

stages:
  - test
  # - deploy

job_runtests:
  stage: test
  script:
    - npm run test
# NEEDS SUDO in shell.. try other env
# job_deploy:
#   stage: deploy
#   script:
#     - gem install dpl
#     - dpl --provider=heroku --app=course-companion --api-key=$HEROKU_PRODUCTION_API_KEY

#   only:
#     - master